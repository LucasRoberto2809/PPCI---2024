#include <bits/stdc++.h>
using namespace std;

void fn(){
    if(
    if(num < x){
        num = num/2;
        meio=0;
        for(int i=0; i<n; i++)
            if(num < a[i])  meio+= num-a[i];
    }
    else{
    }
}

int main(){
    ios::sync_with_stdio(false);
    int t, n, x, a[200005];
    cin >> t;
    while(t-){
        cin >> n >> x;
        int max=0, min=1000000005;
        for(int i=0; i<n; i++){
            cin >> a[i];
            if(max < a[i])  max=a[i];
            if(min > a[i])  min=a[i];
        }
        for(int i=0; i<n; i++){
            if(num < a[i])  meio+= num-a[i];
        }
        fn();
    }


    return 0;
}
